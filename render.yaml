services:
  - type: web
    name: minio-test
    runtime: docker

    plan: free

    disk:
      name: minio-data
      mountPath: /data
      sizeGB: 1

    envVars:
      - key: MINIO_ROOT_USER
        value: admin

      - key: MINIO_ROOT_PASSWORD
        value: strong@Password123